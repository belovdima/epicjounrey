import { ProjectDescription } from "../components/ProjectDescription";

export const AboutPage: React.FC = () => {
    return (
        <div>
            <ProjectDescription />
        </div>
    );
};
